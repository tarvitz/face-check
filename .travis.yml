language: python

python:
  - "3.6"

install:
  - pip install -e src
  - pip install coveralls pyyaml

script:
  - cd src
  - python setup.py test

install:
  - pip install -e .
  - pip install coveralls pyyaml

after_success:
  - coveralls
